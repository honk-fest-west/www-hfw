---
import SocialLinks from '@components/SocialLinks.astro'
import Image from '@components/Image.astro'
import GoldenDino from './GoldenDino.svelte'

import sponsors from '@data/sponsors.json'
import navigation from '@data/navigation.json';

const golden_dino = true;
---
<div class="bg-main-color-400 bg-opacity-80">

  <!-- SPONSORS -->
  <div class="border-t-4 border-b-4 border-main-color-400 ">
    <div class="max-w-screen-xl w-full mx-auto pt-3 px-3 sm:py-4 sm:px-6 flex flex-wrap sm:flex-nowrap">

      <!-- SPONSORS -->
      <div class="flex flex-col items-center w-full mb-2 sm:mb-0">
        <h3 class="font-emphasis text-main-color-50 text-3xl mb-2 sm:mb-3 md:mb-4 tracking-wide">SPONSORS</h3>
        <div class="flex flex-wrap justify-around align-center sm:w-3/4 items-center">
          {sponsors.arts.map((sponsor) => (
            <div class="mx-2 mb-3">
              <a href="{ sponsor.link }" class="rounded block">
                <Image
                  src={ sponsor.image }
                  alt={ sponsor.name }
                  className="rounded w-auto h-14"
                  height="48"
                  tr={ sponsor.tr }
                />
              </a>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</div>

<!-- BOTTOM -->
<div class="max-w-screen-xl w-full mx-auto p-1 sm:p-0 flex flex-wrap sm:flex-nowrap items-center bg-main-color bg-opacity-40">
  <a class="w-full text-center text-main-color-50 text-lg font-bold font-base block w-full sm:w-1/3 m-1 sm:m-4" href="/"
    >© 2022 HONK! Fest West</a
  >
  <ul class="items-center h-full flex-1 w-full sm:w-1/3 m-1 sm:m-4 hidden sm:flex justify-around ">
    {Object.keys(navigation.social).map((item) => (
      <li class="text-main-color-50">
        <SocialLinks link={item} size="1.25rem"/>
      </li>
    ))}
  </ul>
  <GoldenDino client:visible />
  <a class="w-full text-center font-base link text-accent-color font-bold text-lg block w-full sm:w-1/3 m-1 sm:m-4" href="#">
    Back to top
  </a>
</div>