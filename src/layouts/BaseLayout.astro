<!DOCTYPE html>
<html lang="en">

  <!-- HEAD -->
  <head>
    <slot name="head" />
  </head>

  <body class="min-h-full flex flex-col">

    <!-- BACKGROUND IMAGE -->
    <div class="w-full h-full z-0 fixed top-0 left-0">
	    <div class="bg-rainier bg-cover bg-center opacity-40 w-full h-full fixed top-0 left-0"></div>
	    <div class="bg-main-color-300 bg-opacity-60 w-full h-full fixed top-0 left-0"></div>
    </div>

    <!-- HEADER -->
    <header class="z-10">
	    <slot name="header" />
    </header>

    <!-- MAIN -->
	  <main class="z-10 max-w-screen-lg my-8 w-full mx-auto">
		  <slot />
	  </main>

    <!-- FOOTER -->
    <footer class="z-10">
	    <slot name="footer" />
    </footer>
  </body>

</html>
